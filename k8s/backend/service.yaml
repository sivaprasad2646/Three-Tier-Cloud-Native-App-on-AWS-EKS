apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: taskmanager
  labels:
    app: backend           # ServiceMonitor matches this
spec:
  selector:
    app: backend
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP